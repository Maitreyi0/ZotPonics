def test_local_command_insertion():
    """
    Before pulling requests (i.e., command argument pairs) from the database, perform a local test first
    """
    from OverallSystem import OverallSystem
    
    # NOTE: MySQL_ServerCommunicationManager object should be created after overall system
    
    